<UserControl
    x:Class="SensorTag.Controls.TileControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SensorTag.Common"
    xmlns:c="using:SensorTag.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Border BorderThickness="0" Margin="10" Grid.Row="0" Grid.Column="0">
        <c:TiltContentControl x:Name="Tilt">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Image Source="{Binding Icon}" Width="120" Height="120"/>
                <Border Background="#80FFFFFF" CornerRadius="2" HorizontalAlignment="Left" VerticalAlignment="Bottom" Padding="3">
                    <TextBlock Text="{Binding SensorValue}" FontSize="12" Foreground="Black"/>
                </Border>
                <TextBlock Text="{Binding Caption}" Grid.Row="1" HorizontalAlignment="Center" FontSize="18" Foreground="{StaticResource PhoneAccentBrush}"/>
            </Grid>
        </c:TiltContentControl>
    </Border>
</UserControl>
